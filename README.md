brackets-extension-whitelist
============================

Keep only verified extensions in Brackets Extensions Manager.

In order to define which extensions to be listed, edit config.js file (whitelist array).
